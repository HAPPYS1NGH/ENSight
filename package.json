{
  "$schema": "https://www.raycast.com/schemas/extension.json",
  "name": "ens-lookup",
  "title": "ENS Lookup ",
  "description": "Search ENS names and resolve Ethereum addresses without leaving Raycast. ENS <-> Address",
  "icon": "command-icon.png",
  "author": "harpreetsinghxp",
  "license": "MIT",
  "commands": [
    {
      "name": "lookup-ens-name",
      "title": "Lookup ENS Name",
      "subtitle": "Resolve an ENS name to an Ethereum address",
      "description": "Enter an ENS name like vitalik.eth to instantly get the linked wallet address.",
      "mode": "view"
    },
    {
      "name": "reverse-lookup-address",
      "title": "Reverse Lookup Address",
      "subtitle": "Find the ENS name for an Ethereum address.",
      "description": "Enter an Ethereum wallet address and find the linked ENS name, if one is set.",
      "mode": "view"
    }
  ],
  "dependencies": {
    "@raycast/api": "^1.100.3",
    "@raycast/utils": "^2.2.0",
    "ethers": "^6.15.0"
  },
  "devDependencies": {
    "@raycast/eslint-config": "^1.0.6",
    "@types/node": "20.8.10",
    "eslint": "^8.51.0",
    "prettier": "^3.0.3",
    "typescript": "^5.2.2"
  },
  "scripts": {
    "build": "ray build -e dist",
    "dev": "ray develop",
    "fix-lint": "ray lint --fix",
    "lint": "ray lint",
    "publish": "npx @raycast/api@latest publish"
  }
}
